1.  Не нашёл как прописать путь к requirements.txt в Dockerfile, eсли файл лежит в папке на уровень выше. 
    Вариант с 'COPY ../requirements.txt ./tmp/requirements.txt' не сработал
    Пришлось положить файл requirements.txt в папку part1_airflow. 
    Можно ли как-то обойти эту проблему и оставить requirements.txt в корневом каталоге проекта?

2.  Схожая проблема на тему указания относительного пути:
    В плагине для отправки сообщений в Telegram я вынес token и chat_id в .env. Однако, мне не удалось 
    указать относительный путь так, чтобы load_env() считывал .env, лежащий в папке mle-project-sprint-1, поэтому
    я положил в папку telegram_informer ещё один .env, который содержит только данные о боте. 
    Бот работает, сообщения приходят.
    Путь к плагину:
    /home/mle-user/mle_projects/mle-project-sprint-1/part1_airflow/plugins/telegram_informer/messages.py

3.  В качестве базовой модели я выбрал модель линейной регрессии. Как и было указано в упражнениях, я не стал 
    проводить train/test split, а вместо этого применил кросс-валидацию.